//sample

#include<stdio.h>
int main()
{
    int i,size,arr[size];
    printf("Enter size of array : ");
    scanf("%d",&size);
    // int arr[size];
    for(i=0;i<size;i++)
    {
        scanf("%d",&arr[i]);
    }
    return 0;
}